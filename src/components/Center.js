const Center = ({ children }) => {
  return (
    <div
      style={{
        maxWidth: "40rem",
        margin: "2rem auto 6rem",
      }}
    >
      {children}
    </div>
  );
};

export default Center;
